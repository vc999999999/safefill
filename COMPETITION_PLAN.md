# 隐填 3.0 · 参赛叙事

## 一句话定位

**把在线协作平台从“身份明文数据库”变成“密文运输与数据最小化进度面板”：员工离线加密提交，AI 只协调密文和必要的姓名/员工号状态，授权管理者在 Intel AI PC 本地通过 OpenVINO 复核。**

## 演示主线

1. 从 `employee_id,name` 名单批量生成个性化离线邀请。
2. 员工在 Chrome/Edge 手填并添加身份证照片，页面断网运行。
3. 展示遮罩预览和告知确认，再导出 `.yintian`。
4. 打开密文文件证明平台和 AI 看不到手机号、身份证号和附件。
5. 管理端输入任务密码，本地解密并用 OpenVINO OCR 复核。
6. 故意让手填身份证号与附件 OCR 冲突，系统只标记复核，不覆盖数据。
7. 生成不含表单敏感值的 XLSX/JSON；它仍含姓名和员工号，只上传到获批且访问受控的平台。
8. 到期后执行二次确认清理，只保留匿名统计。

## 关键卖点

- Web Crypto 与 Python `cryptography` 跨端兼容的标准信封加密。
- 员工零安装、无需后端、无需云端 OCR。
- OpenVINO 只在授权管理端处理解密后的附件。
- AI/MCP 默认无明文接口，`reveal` 被隔离为人工终端操作。
- 每邀请独立认证令牌、多版本、去重、无效提交不覆盖旧有效版本、截止和保存期限控制。

## 提交前实测

- Intel CPU/GPU/NPU 的 OCR 冷启动和热启动延迟。
- Chrome 和 Edge 各完成一次离线加密与 Python 解密。
- 至少 10 组脱敏证件测试 OCR 匹配、冲突和低置信度。
- 扫描生成报告，确认无手机号、身份证、地址、邀请认证令牌、OCR 原文和密钥。
- 说明 `.yintian-task` 是含明文名单标识的普通任务交接 ZIP，只能走已认证且加密的渠道。
