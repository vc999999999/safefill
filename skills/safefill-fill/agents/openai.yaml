interface:
  display_name: "SafeFill · 填写者"
  short_description: "读取 AI 请求包，对话生成加密回执"
  default_prompt: "使用 $safefill-fill：读取 REQUEST.yintian-request，在对话中一次问齐缺失字段并生成姓名可读、内容加密的回执；不要创建或展示 HTML 表单，也不要让我运行终端。"
