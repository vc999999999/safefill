# 4.2.0 验证记录 · 2026-09-09

- 真实创建无需名单的 `yintian-request/1` 机器请求包；默认配置只含必填姓名，不生成 HTML、个人凭据或人工任务密码；4.1 的 `yintian-form/3` 仅兼容读取。
- 两名合成员工生成 `姓名-随机短码.yintian`；姓名不出现在信封正文，临时 answers 权限过宽会拒绝且始终删除。
- 收集端一键生成中文表头 Excel；图片解密到同名附件目录，Excel 相对路径和原始字节回读一致。
- 员工携带旧回执更正后沿用同一随机编号；历史保留，Excel 只采用最新通过版本。
- 开放任务私钥文件保持加密，本地密钥位于任务目录外且为 `0600`；加密交接包导入后公私钥匹配并可继续导出。
- 字段、选项、标量、附件数、单次收件、开放回执数和任务密文总量均有边界；协议外信封与解密载荷字段拒绝。
- 全套回归：168 项测试、17 项子用例通过；两个 Skill 快速校验、浏览器合同和 49 文件交付包完整性检查通过。

开放请求包身份为自报，不声称验证自然人身份，也不能阻止请求包转发或垃圾提交。旧定向与名单凭据模式仅保留兼容。
